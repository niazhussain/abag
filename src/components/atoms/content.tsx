import styled from 'styled-components';
import { theme } from '../../theme';

export const Content = styled.main`
  max-width: 1440px;
  padding: ${theme.spacing.unit * 8}px ${theme.spacing.unit * 3}px 0;
  margin: 0 auto;
`;
